FROM node:22-alpine
WORKDIR /app

# Copiar build
COPY .output /app/.output
COPY public /app/public
COPY package.json /app/

# Instalar apenas produção
RUN npm install --production --legacy-peer-deps 2>/dev/null || true

EXPOSE 3000

CMD ["node", ".output/server/index.mjs"]
